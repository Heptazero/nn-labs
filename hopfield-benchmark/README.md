# Hopfield Benchmark

这个目录按“任务”而不是按“论文”组织 Hopfield 模型比较。

论文复现 notebook 继续保留，负责证明某个模型实现忠于原文；这里的统一基准负责让不同模型在相同数据、线索、随机种子、停止规则和资源预算下比较。

统一实验继续使用现有 notebook 的组件式管线：

`a 记忆输入 → b 模型存储 → c 检索线索 → d 检索动力学 → e 测量 → f 同图比较`

只要 `a/c/e/f` 保持不变，替换 `b/d` 就是在同一任务中替换模型。因此，同一张图上的每条线只代表一个模型，不会混入不同数据或不同噪声协议。

先看 [问题目录与实施计划](./QUESTIONS.md)：列出 13 个问题、已有入口、缺口、模型位置、分文件方案和 Colab 起步预算。

当前状态（2026-09-05）：六个模型已提取到 [共享模型目录](../am_bench/models/)，数据、测量、运行器和绘图也已拆分。[第一阶段 Notebook](./hopfield_benchmark_phase1_colab.ipynb) 保留原有实验章节，代码从约 1962 行缩减到 895 行；专属机制补充仍内联。两个入口都自动加载固定提交，用户只打开一个 Notebook 即可。完整比较约定见 [PROTOCOL.md](./PROTOCOL.md)。

## 先运行 Q04：反复检索

[在 Colab 打开](https://colab.research.google.com/github/Heptazero/nn-labs/blob/main/hopfield-benchmark/retrieval_dynamics.ipynb) · [查看 Notebook](./retrieval_dynamics.ipynb)

1. 保存副本到 Drive，从头运行。入口先检查六个模型拆分前后的小样本行为，再执行实验。
2. 默认 Classical、三阶 Polynomial DAM、连续迭代 Modern 三个模型，共 432 次检索、12 个批次、3888 行逐轮记录，CPU float64。它是接口与计时用的小扫描，未调优，不用于正式结论。
3. 默认结果目录为 Drive 的 `MyDrive/nn-labs-results/q04-smoke-v1`。同配置、同代码和同运行环境重跑会恢复已有批次；换配置或依赖版本需换 `RUN_NAME`。一个目录只允许一个运行时写入。
4. 先看含 t=0 的共同识别率与数据可用比例，再看错→对/对→错及各自分母。固定点后的保持与失败缺测分开记录。附带原生成本表；尚未完成等计算预算的逐轮对比。

源码在 [dynamics.py](../am_bench/dynamics.py)；模型接收查询和观察器，正确答案由外部测量器持有。Modern 单步与连续迭代共用更新函数，但登记为不同检索配置。Q05 所需的权重熵、最大权重已保存，正式分析尚未实现；FY、双曲模型尚未接入。

本地验证：模块/Notebook 语法、跨文件名称及调用检查通过；断点保存的 5 项标准库测试通过。**未在本地执行 PyTorch 实验，也未代替用户执行 Colab。** 拆分前后 36 个数值对照与 Q04 数值/恢复检查已写入 [validation.py](../am_bench/validation.py)，状态仍为待 Colab 运行。无已验证的模型排名或机制结论。

首次 Run All 优先使用 Q04。综合 Notebook 的主扫描为 2304 次检索，后面还有附加实验，结果在主扫描结束后写入临时运行时的独立目录；它暂不具备 Q04 的完整分批恢复功能。

## 计划中的结果图

1. 固定存储量下：横轴为噪声强度，纵轴为召回率或 overlap，每条线一个模型。
2. 固定网络规模下：横轴为存储模式数 `P`，纵轴为召回率，每条线一个模型。
3. 容量缩放：横轴为 `N`，纵轴为经验临界容量 `P_c(N)`；`P_c` 使用对数纵轴。
4. 动力学：相同初态下比较能量、误差和收敛状态；没有对应能量的模型显示 `N/A`，不套用经典二次能量。
5. 资源 Pareto 图：横轴为参数、显存或 FLOPs，纵轴为容量或鲁棒召回率。

任何跨模型主图都必须满足 [PROTOCOL.md](./PROTOCOL.md) 中的“同图资格检查”。
